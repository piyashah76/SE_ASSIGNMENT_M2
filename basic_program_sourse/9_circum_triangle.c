#include<stdio.h>
main()
{
	int a,b,c;
	printf("Enter a side of a rectangle:");
	scanf("%d",&a);
	printf("Enter a side of a rectangle:");
	scanf("%d",&b);
	printf("Enter a side of a rectangle:");
	scanf("%d",&c);
	printf("Circumference of triangle is:%d",(a+b+c));
}
